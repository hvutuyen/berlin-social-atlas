name: 'berlin_social_atlas'
version: '1.0.0'
config-version: 2

profile: 'berlin_social_atlas'

model-paths: ["models"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  berlin_social_atlas:
    staging:
      +materialized: view
    marts:
      +materialized: table
